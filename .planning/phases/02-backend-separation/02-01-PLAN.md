---
phase: 02-backend-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/api.ts
  - vitest.config.ts
  - server/services/analysis.ts
  - server/services/library.ts
  - server/services/chunks.ts
  - server/services/analysis.test.ts
  - server/services/library.test.ts
  - server/services/chunks.test.ts
autonomous: true
requirements: [BEND-03, BEND-04]

must_haves:
  truths:
    - "Service functions can be called directly in a test without HTTP server"
    - "getAnalysisById(id) returns a cached analysis or null"
    - "All service functions accept plain typed arguments and return typed values"
  artifacts:
    - path: "src/types/api.ts"
      provides: "Request/response contract types for all endpoints"
      exports: ["CheckSentenceRequest", "SaveSentenceRequest", "SaveChunkRequest", "SaveToLibraryRequest", "AnalysisResponse", "ErrorResponse", "SuccessResponse", "HistoryEntry"]
    - path: "server/services/analysis.ts"
      provides: "Analysis business logic (checkSentence, saveSentence, getAnalysisById)"
      exports: ["checkSentence", "saveSentence", "getAnalysisById"]
    - path: "server/services/library.ts"
      provides: "Library business logic (saveToLibrary, getSaved, getHistory)"
      exports: ["saveToLibrary", "getSaved", "getHistory"]
    - path: "server/services/chunks.ts"
      provides: "Chunk business logic (saveChunk, getChunks)"
      exports: ["saveChunk", "getChunks"]
    - path: "vitest.config.ts"
      provides: "Test runner discovers server/**/*.test.ts files"
      contains: "server/**/*.test.ts"
  key_links:
    - from: "server/services/analysis.ts"
      to: "src/db/index.js"
      via: "import { db }"
      pattern: "import.*db.*from.*src/db/index"
    - from: "server/services/analysis.ts"
      to: "src/types/index.js"
      via: "import { SentenceRow, AnalysisResult }"
      pattern: "import.*SentenceRow.*from.*src/types"
    - from: "server/services/analysis.test.ts"
      to: "server/services/analysis.js"
      via: "direct import of service functions"
      pattern: "import.*from.*\\./analysis"
---

<objective>
Create the service layer and API contract types for the backend separation.

Purpose: Establish the independently testable service functions (BEND-04) and the GET /api/analysis/:id logic (BEND-03) before controllers or routes exist. This lets us verify business logic correctness with unit tests before wiring the HTTP layer.
Output: 3 service files with unit tests, API contract types, updated vitest config.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-separation/02-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from Phase 1 -->

From src/types/analysis.ts:
```typescript
export type AnalysisResult = z.infer<typeof AnalysisResultSchema>;
// Contains: sentence, sentence_type, main_clause, core_skeleton, components,
// structure_tree, meaning, key_points, chunks, review_summary, quiz
```

From src/types/database.ts:
```typescript
export interface SentenceRow {
  id: number;
  text: string;
  analysis_json: string;
  created_at: string;
}
export interface SavedSentenceRow { ... }
export interface ChunkRow {
  id: number;
  expression: string;
  meaning: string;
  pattern: string | null;
  examples: string | null; // JSON-serialized string[]
  source_sentence_id: number | null;
  tags: string | null;
  review_status: string;
  created_at: string;
}
```

From src/db/index.ts:
```typescript
export const db = new Database(dbPath);
// Supports TEST_DB=":memory:" env var for test isolation
```

From src/types/index.ts:
```typescript
export * from "./analysis.js";
export * from "./database.js";
export * from "./api.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define API contract types and create service layer</name>
  <files>src/types/api.ts, server/services/analysis.ts, server/services/library.ts, server/services/chunks.ts</files>
  <action>
1. **Populate `src/types/api.ts`** with request/response contract types (replace the `export {}` placeholder):
   - `CheckSentenceRequest { sentence: string }`
   - `SaveSentenceRequest { sentence: string; analysis: AnalysisResult }` (import AnalysisResult from `./analysis.js`)
   - `SaveToLibraryRequest { sentence: string }`
   - `SaveChunkRequest { expression: string; meaning: string; examples: string[] }`
   - `AnalysisResponse` — re-export or extend AnalysisResult
   - `ErrorResponse { error: string }`
   - `SuccessResponse { success: boolean; message?: string }`
   - `HistoryEntry { id: number; text: string; created_at: string }`

2. **Create `server/services/analysis.ts`** with three exported functions:
   - `checkSentence(sentence: string): AnalysisResult | null` — queries `sentences` table by text, returns parsed analysis_json or null
   - `saveSentence(sentence: string, analysis: AnalysisResult): void` — inserts into sentences table
   - `getAnalysisById(id: number): (AnalysisResult & { id: number; sentence: string }) | null` — queries by primary key, returns parsed analysis_json with id and sentence text, or null. This is the new BEND-03 endpoint logic.
   - Import `db` from `../../src/db/index.js` and types from `../../src/types/index.js` (use RELATIVE paths, NOT `@/` alias — see Pitfall 3 in research)
   - All imports must use `.js` extension (ESM — see Pitfall 2)
   - NO Express imports allowed in this file (BEND-04 compliance)

3. **Create `server/services/library.ts`** with three exported functions:
   - `saveToLibrary(sentence: string): { success: boolean; message?: string }` — finds sentence by text, checks if already saved, inserts into saved_sentences if not
   - `getSaved(): Array<{ id: number; text: string; analysis_json: string; created_at: string }>` — joins saved_sentences with sentences, returns ordered by created_at DESC
   - `getHistory(): HistoryEntry[]` — selects id, text, created_at from sentences, limit 10, ordered by created_at DESC
   - Import `db` from `../../src/db/index.js`, types from `../../src/types/index.js`
   - NO Express imports

4. **Create `server/services/chunks.ts`** with two exported functions:
   - `saveChunk(expression: string, meaning: string, examples: string[]): void` — inserts into chunks table with JSON.stringify(examples)
   - `getChunks(): ChunkRow[]` — selects all chunks ordered by created_at DESC
   - Import `db` from `../../src/db/index.js`, `ChunkRow` from `../../src/types/index.js`
   - NO Express imports

Extract the exact SQL queries from current `server.ts` — do not rewrite them. The logic must be identical to the existing inline handlers.
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/sentence-scaffold && npx tsc --noEmit</automated>
  </verify>
  <done>All 4 files exist, type-check passes, no Express imports in any service file, API types are exported from barrel via src/types/index.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update vitest config and write service unit tests</name>
  <files>vitest.config.ts, server/services/analysis.test.ts, server/services/library.test.ts, server/services/chunks.test.ts</files>
  <behavior>
    - getAnalysisById(9999) returns null
    - getAnalysisById(valid_id) returns parsed AnalysisResult with id and sentence
    - checkSentence("nonexistent") returns null
    - checkSentence(existing_text) returns parsed AnalysisResult
    - saveSentence inserts a row retrievable by checkSentence
    - saveToLibrary for unknown sentence returns { success: false } or throws — match existing server.ts behavior (returns 404-equivalent)
    - saveToLibrary for known sentence inserts into saved_sentences
    - saveToLibrary for already-saved sentence returns { success: true, message: "Already saved" }
    - getSaved returns joined data from saved_sentences + sentences
    - getHistory returns recent sentences ordered by created_at DESC, limit 10
    - saveChunk inserts a chunk row
    - getChunks returns all chunks ordered by created_at DESC
  </behavior>
  <action>
1. **Update `vitest.config.ts`**: Change `include` from `["src/**/*.test.ts"]` to `["src/**/*.test.ts", "server/**/*.test.ts"]`.

2. **Create `server/services/analysis.test.ts`**:
   - Import `describe, it, expect, beforeEach` from vitest
   - Import `db` from `../../src/db/index.js`
   - Import `checkSentence, saveSentence, getAnalysisById` from `./analysis.js`
   - Use `beforeEach(() => { db.exec("DELETE FROM sentences"); })` for isolation
   - Create a `MOCK_ANALYSIS` constant matching AnalysisResult shape (use the example from research doc)
   - Tests: getAnalysisById unknown returns null, saveSentence + getAnalysisById roundtrip, checkSentence unknown returns null, saveSentence + checkSentence roundtrip

3. **Create `server/services/library.test.ts`**:
   - Same beforeEach cleanup pattern (delete from saved_sentences AND sentences)
   - Tests: saveToLibrary with unknown sentence handling, saveToLibrary + getSaved roundtrip, already-saved dedup, getHistory returns recent entries

4. **Create `server/services/chunks.test.ts`**:
   - beforeEach cleanup (delete from chunks)
   - Tests: saveChunk + getChunks roundtrip, getChunks returns empty array initially, examples are JSON-stringified in DB

All tests use in-memory SQLite (TEST_DB=:memory: set in vitest.config.ts). No HTTP server needed — this proves BEND-04.
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/sentence-scaffold && npx vitest run server/ --reporter=verbose</automated>
  </verify>
  <done>All service tests pass. vitest discovers and runs server/**/*.test.ts files. Services are proven callable without HTTP server (BEND-04).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (type safety across all new files)
- `npx vitest run server/` runs all service tests and they pass
- `grep -r "express" server/services/` returns no matches (BEND-04: no Express dependency in services)
- `getAnalysisById` function exists and is tested (BEND-03 logic ready)
</verification>

<success_criteria>
- 3 service files exist in server/services/ with pure business logic functions
- API contract types populated in src/types/api.ts
- All service unit tests pass with in-memory SQLite
- No Express imports in any service file
- Type checking passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-separation/02-01-SUMMARY.md`
</output>
