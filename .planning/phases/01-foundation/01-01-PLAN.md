---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/types/analysis.ts
  - src/types/database.ts
  - src/types/api.ts
  - src/types/index.ts
  - src/db/index.ts
  - src/db/index.test.ts
  - src/types/analysis.test.ts
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TEST-01

must_haves:
  truths:
    - "Zod schema validates a real Gemini response snapshot without error"
    - "DB smoke test inserts and retrieves a row using in-memory SQLite"
    - "All data structures have explicit TypeScript interfaces (no any)"
    - "vitest run executes and all tests pass"
  artifacts:
    - path: "src/types/analysis.ts"
      provides: "AnalysisResultSchema Zod schema + AnalysisResult type via z.infer<>"
      exports: ["AnalysisResultSchema", "AnalysisResult", "ComponentSchema"]
    - path: "src/types/database.ts"
      provides: "Plain TS interfaces for all 3 DB tables"
      exports: ["SentenceRow", "SavedSentenceRow", "ChunkRow"]
    - path: "src/types/api.ts"
      provides: "API contract type stubs for Phase 2"
      exports: []
    - path: "src/types/index.ts"
      provides: "Barrel re-export of all types"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with node environment and TEST_DB env"
    - path: "src/types/analysis.test.ts"
      provides: "Zod schema smoke test against real response snapshot"
    - path: "src/db/index.test.ts"
      provides: "DB CRUD smoke test with in-memory SQLite"
  key_links:
    - from: "src/types/analysis.ts"
      to: "zod"
      via: "z.infer<typeof AnalysisResultSchema>"
      pattern: "z\\.infer"
    - from: "src/db/index.ts"
      to: "process.env.TEST_DB"
      via: "env var check before Database construction"
      pattern: "process\\.env\\.TEST_DB"
    - from: "vitest.config.ts"
      to: "src/db/index.ts"
      via: "TEST_DB env var injection"
      pattern: "TEST_DB.*:memory:"
---

<objective>
Install Zod and Vitest, create all TypeScript type definitions and Zod schemas, configure test infrastructure, and write two smoke tests.

Purpose: Establishes the type-safe foundation (interfaces, schemas, test runner) that Plan 02 (strict mode) and all subsequent phases depend on.
Output: `src/types/` directory with domain-organized types, working vitest with two passing smoke tests, db singleton with :memory: injection.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@src/services/ai.ts
@src/db/index.ts
@server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create type definitions, and configure Vitest</name>
  <files>package.json, vitest.config.ts, src/types/analysis.ts, src/types/database.ts, src/types/api.ts, src/types/index.ts</files>
  <action>
1. Install dependencies:
   ```
   npm install zod
   npm install -D vitest @types/better-sqlite3
   ```

2. Add test script to package.json scripts: `"test": "vitest run"`

3. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from "vitest/config";
   export default defineConfig({
     test: {
       environment: "node",
       include: ["src/**/*.test.ts"],
       env: { TEST_DB: ":memory:" },
     },
   });
   ```

4. Create `src/types/analysis.ts` — Zod schema mirroring the Google GenAI `analysisSchema` in `src/services/ai.ts`:
   - Define `ComponentSchema` with fields: text (string), role (string), modifies (string optional), explains (string optional)
   - Define `AnalysisResultSchema` with all top-level fields from `ai.ts` analysisSchema:
     - sentence (string), sentence_type (object: category, summary), main_clause (object: subject, verb, complement optional)
     - core_skeleton (string), components (array of ComponentSchema), structure_tree (array with label + optional children array)
     - meaning (object: literal_cn, natural_cn), key_points (array of {point}), chunks (array of {expression, meaning, examples: string[]})
     - review_summary (object: look_first, easy_to_misread, how_to_parse_next_time), quiz (array of {question, reference_answer})
   - Mark `complement`, `modifies`, `explains`, `children` as `.optional()` — these are not required in the GenAI schema
   - Export `type AnalysisResult = z.infer<typeof AnalysisResultSchema>`

5. Create `src/types/database.ts` — plain TypeScript interfaces derived from `src/db/index.ts` DDL:
   - `SentenceRow`: id (number), text (string), analysis_json (string), created_at (string)
   - `SavedSentenceRow`: id (number), sentence_id (number), tags (string | null), notes (string | null), review_status (string), created_at (string)
   - `ChunkRow`: id (number), expression (string), meaning (string), pattern (string | null), examples (string | null), source_sentence_id (number | null), tags (string | null), review_status (string), created_at (string)

6. Create `src/types/api.ts` — minimal stub with a comment that request/response contracts are added in Phase 2:
   ```typescript
   // API request/response contract types — populated in Phase 2
   // Placeholder to establish the file in the barrel export
   export {};
   ```

7. Create `src/types/index.ts` — barrel re-export:
   ```typescript
   export * from "./analysis.js";
   export * from "./database.js";
   export * from "./api.js";
   ```
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/sentence-scaffold && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All type files exist and export correctly. Vitest config exists. Dependencies installed. `tsc --noEmit` runs without errors on the new type files (existing server.ts errors are expected and will be fixed in Plan 02).</done>
</task>

<task type="auto">
  <name>Task 2: Add DB :memory: injection and write smoke tests</name>
  <files>src/db/index.ts, src/db/index.test.ts, src/types/analysis.test.ts</files>
  <action>
1. Modify `src/db/index.ts` to support in-memory SQLite for tests:
   - Add env var check BEFORE the `new Database()` call: `const dbPath = process.env.TEST_DB === ":memory:" ? ":memory:" : path.join(process.cwd(), "data", "app.db")`
   - Only create the `data/` directory if `dbPath !== ":memory:"`
   - Pass `dbPath` to `new Database(dbPath)` instead of the hardcoded path
   - Keep all DDL (CREATE TABLE) unchanged — it runs on both real and :memory: databases

2. Create `src/types/analysis.test.ts` — Zod schema smoke test:
   - Import `AnalysisResultSchema` from `./analysis.js`
   - Define a `REAL_GEMINI_RESPONSE_SNAPSHOT` constant that represents a complete valid Gemini response (use the fixture from RESEARCH.md as the base, ensure all required fields are present)
   - Test case 1: "validates a complete Gemini response snapshot without error" — `expect(() => AnalysisResultSchema.parse(REAL_GEMINI_RESPONSE_SNAPSHOT)).not.toThrow()`
   - Test case 2: "rejects a response missing required fields" — remove `sentence` from snapshot, expect `.parse()` to throw
   - Test case 3: "accepts a response with optional fields omitted" — remove `complement` from `main_clause`, remove `modifies` from components — should NOT throw

3. Create `src/db/index.test.ts` — DB CRUD smoke test:
   - Import `db` from `./index.js` (vitest.config.ts sets TEST_DB=:memory: so this will use in-memory DB)
   - Import `SentenceRow` from `../types/index.js`
   - Test case 1: "inserts and retrieves a sentence row" — INSERT a row, SELECT it back, assert text matches
   - Test case 2: "returns undefined for non-existent row" — `.get()` with non-existent id, assert result is undefined
   - Use `as SentenceRow | undefined` on `.get()` results to demonstrate the typed pattern

4. Run `npx vitest run` to verify all tests pass.
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/sentence-scaffold && npx vitest run 2>&1</automated>
  </verify>
  <done>Both smoke tests pass: Zod schema validates the snapshot (TYPE-02), DB CRUD works with in-memory SQLite (TEST-01). No real `data/app.db` file is created during test run. `vitest run` exits 0 with at least 4 passing tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all smoke tests pass (green exit code)
2. `ls src/types/` — analysis.ts, database.ts, api.ts, index.ts all exist
3. `grep "z.infer" src/types/analysis.ts` — confirms TYPE-03 (type derived from schema)
4. `grep "TEST_DB" src/db/index.ts` — confirms :memory: injection point exists
5. No `data/app.db` file created during test run
</verification>

<success_criteria>
- TYPE-01: All data structures (SentenceRow, SavedSentenceRow, ChunkRow, AnalysisResult) have explicit interfaces in src/types/
- TYPE-02: Zod schema validates real Gemini response snapshot without error
- TYPE-03: AnalysisResult type derived via z.infer<typeof AnalysisResultSchema>
- TYPE-04: Database row interfaces exported and usable for typed queries (demonstrated in test)
- TEST-01: vitest run executes at least 4 passing smoke tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
