---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tsconfig.json
  - src/services/ai.ts
  - server.ts
autonomous: true
requirements:
  - TYPE-05

must_haves:
  truths:
    - "strict: true is enabled in tsconfig.json and the project compiles with zero errors"
    - "analyzeSentence() returns a Zod-validated AnalysisResult (not raw JSON.parse)"
    - "All DB query results in server.ts use typed row interfaces instead of 'as any'"
  artifacts:
    - path: "tsconfig.json"
      provides: "strict: true enabled"
      contains: "\"strict\": true"
    - path: "src/services/ai.ts"
      provides: "Zod parse integration at API boundary"
      contains: "AnalysisResultSchema.parse"
    - path: "server.ts"
      provides: "Typed DB queries with SentenceRow instead of any"
      contains: "SentenceRow"
  key_links:
    - from: "src/services/ai.ts"
      to: "src/types/analysis.ts"
      via: "import AnalysisResultSchema"
      pattern: "import.*AnalysisResultSchema.*from.*types"
    - from: "server.ts"
      to: "src/types/database.ts"
      via: "import SentenceRow"
      pattern: "import.*SentenceRow.*from.*types"
---

<objective>
Enable TypeScript strict mode, fix all resulting errors, and integrate Zod validation into the Gemini API boundary.

Purpose: Completes the type-safety foundation — strict mode catches implicit any, null issues at compile time; Zod parse provides runtime validation at the external data boundary.
Output: Zero-error strict-mode compilation, Zod-validated AI responses, typed DB queries throughout server.ts.
</objective>

<execution_context>
@/home/wuy6/.claude/get-shit-done/workflows/execute-plan.md
@/home/wuy6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@src/services/ai.ts
@server.ts
@tsconfig.json

<interfaces>
<!-- From Plan 01 outputs — types the executor will import -->

From src/types/analysis.ts:
```typescript
export const AnalysisResultSchema: z.ZodObject<...>;
export type AnalysisResult = z.infer<typeof AnalysisResultSchema>;
```

From src/types/database.ts:
```typescript
export interface SentenceRow {
  id: number;
  text: string;
  analysis_json: string;
  created_at: string;
}
export interface SavedSentenceRow { ... }
export interface ChunkRow { ... }
```

From src/types/index.ts:
```typescript
export * from "./analysis.js";
export * from "./database.js";
export * from "./api.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable strict mode and fix all compilation errors</name>
  <files>tsconfig.json, src/services/ai.ts, server.ts</files>
  <action>
1. First, run `npx tsc --noEmit --strict` to get the full error list BEFORE modifying tsconfig. Record the count and categorize errors.

2. Add `"strict": true` to tsconfig.json compilerOptions.

3. Fix `src/services/ai.ts`:
   - Add import: `import { AnalysisResultSchema, type AnalysisResult } from "../types/index.js";`
   - Change return type: `export async function analyzeSentence(sentence: string): Promise<AnalysisResult>`
   - Replace `return JSON.parse(text)` with `return AnalysisResultSchema.parse(JSON.parse(text))`

4. Fix `server.ts` — systematically replace every `as any` and `error: any`:
   - Add import: `import { SentenceRow } from './src/types/index.js';`
   - `/api/check-sentence`: Change `db.prepare(...).get(sentence) as any` to `db.prepare(...).get(sentence) as SentenceRow | undefined`
   - `/api/save`: Change `db.prepare(...).get(sentence) as any` to `db.prepare(...).get(sentence) as { id: number } | undefined`
   - All `catch (error: any)` blocks: Change to `catch (error: unknown)` and replace `error.message` with `error instanceof Error ? error.message : String(error)`
   - Express route handler return type issues: If `return res.status(...).json(...)` causes "not assignable to void" errors, change to `res.status(...).json(...); return;` pattern or add `as void` — use whichever approach fixes the specific strict-mode complaint
   - For any remaining errors that genuinely require Phase 2 types, use `// @ts-expect-error // TODO(phase-2)` — but exhaust all Phase 1 fixes first

5. Run `npx tsc --noEmit` to verify zero errors.

6. Run `npx vitest run` to verify smoke tests still pass (regression check).

IMPORTANT: Use `@ts-expect-error` NOT `@ts-ignore`. Use `// TODO(phase-N)` comment on every suppression to track forward references.
  </action>
  <verify>
    <automated>cd /home/wuy6/myprojects/sentence-scaffold && npx tsc --noEmit 2>&1 && npx vitest run 2>&1</automated>
  </verify>
  <done>tsconfig.json has `"strict": true`. `tsc --noEmit` exits 0 with zero errors. `vitest run` still passes all smoke tests. No `as any` remains in server.ts or ai.ts. `analyzeSentence()` returns `Promise<AnalysisResult>` with Zod validation. All catch blocks use `error: unknown` pattern.</done>
</task>

</tasks>

<verification>
1. `grep '"strict"' tsconfig.json` — shows `"strict": true`
2. `npx tsc --noEmit` — zero errors
3. `npx vitest run` — all tests pass
4. `grep -n "as any" server.ts src/services/ai.ts` — zero matches
5. `grep -n "error: any" server.ts src/services/ai.ts` — zero matches
6. `grep -n "AnalysisResultSchema.parse" src/services/ai.ts` — confirms Zod integration
7. `grep -n "SentenceRow" server.ts` — confirms typed DB queries
</verification>

<success_criteria>
- TYPE-05: `strict: true` enabled in tsconfig.json, project compiles with zero errors, zero `any` types in modified files
- Zod parse integrated at API boundary (ai.ts)
- All DB queries in server.ts use typed row interfaces
- All catch blocks use `error: unknown` pattern
- All existing smoke tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
